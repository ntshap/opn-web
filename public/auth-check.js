// Client-side authentication check for Netlify static export
// DISABLED to prevent redirect loops
(function() {
  console.log('Auth check script disabled to prevent redirect loops');
  
  // Original authentication check code removed
  
  // No redirects will happen from this script
})();
